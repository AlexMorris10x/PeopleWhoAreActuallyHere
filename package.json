{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "start": "npx lerna run --parallel start",
    "build": "npx lerna run --parallel build",
    "prod": "NODE_ENV=production node $BUILD_DIR/web-api/bin/main.js"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "license": "AGPL-3.0-or-later"
}
